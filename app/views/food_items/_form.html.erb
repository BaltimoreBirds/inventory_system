<%= form_for @food_item do |f| %>
  <% if @food_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@food_item.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @food_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :title, 'Title' %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description, 'Description' %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :quantity, 'Quantity' %><br />
    <%= f.text_field :quantity %>
  </div>
  <div class="actions">
    <%= f.submit 'Enter New Inventory' %>
  </div>
<% end %>
